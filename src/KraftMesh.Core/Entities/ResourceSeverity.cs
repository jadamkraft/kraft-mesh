namespace KraftMesh.Core.Entities;

/// <summary>
/// Urgency/severity of a need (or optional classification for haves).
/// </summary>
public enum ResourceSeverity
{
    Low = 0,
    Medium = 1,
    High = 2,
    Critical = 3,
}
